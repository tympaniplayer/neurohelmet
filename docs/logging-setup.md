# Game log — moved

This content now lives in the Neurohelmet documentation site (mdBook), so there's a single canonical
copy to maintain:

- **Guide:** [docs/guide/src/guides/game-log.md](guide/src/guides/game-log.md) — capture, local export,
  publishing (GitHub CLI *or* your own repo), and `log_auto_push`.
- **Config keys:** [docs/guide/src/reference/configuration.md](guide/src/reference/configuration.md).

This pointer is kept so older links don't break.
